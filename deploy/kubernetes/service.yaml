apiVersion: v1
kind: Service
metadata:
  name: idaten-backend
  namespace: idaten
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: idaten
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: http
